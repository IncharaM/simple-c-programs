#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,i,sum,remainder;
    scanf("%d", &n);
    for(i=n;i>=n;i--)
    {
        remainder=n%10;
        n=n/10;
        sum=sum+remainder;
    }
    printf("%d",sum);
    return 0;
}
